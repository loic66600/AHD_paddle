'use strict';

var themeLightStateFocus = require('../theme/themeLightStateFocus.js');
var borderRadiusSmall = require('../border/borderRadiusSmall.js');
var borderRadiusMedium = require('../border/borderRadiusMedium.js');
var borderWidthBase = require('../border/borderWidthBase.js');

const getFocusStyle = (opts) => {
    const outlineOffset = opts?.offset === 'small' ? '2px' : opts?.offset === 'none' ? 0 : opts?.offset || '2px';
    const borderRadius = opts?.borderRadius === 'small'
        ? borderRadiusSmall.borderRadiusSmall
        : opts?.borderRadius === 'medium'
            ? borderRadiusMedium.borderRadiusMedium
            : opts?.borderRadius || borderRadiusSmall.borderRadiusSmall;
    return {
        borderRadius,
        '&:focus': {
            outline: `${borderWidthBase.borderWidthBase} solid ${themeLightStateFocus.themeLightStateFocus}`,
            outlineOffset,
        },
        // why? have a look at this article https://www.tpgi.com/focus-visible-and-backwards-compatibility/
        '&:focus:not(:focus-visible)': {
            outlineColor: 'transparent',
        },
    };
};

exports.getFocusStyle = getFocusStyle;
