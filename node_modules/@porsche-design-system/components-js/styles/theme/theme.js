'use strict';

var themeLight = require('./themeLight.js');
var themeDark = require('./themeDark.js');

const theme = {
    light: themeLight.themeLight,
    dark: themeDark.themeDark,
};

exports.theme = theme;
